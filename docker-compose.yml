version: "3.9"

services:
  koraos-runtime:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: koraos-runtime
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      APP_VERSION: local
    restart: unless-stopped
